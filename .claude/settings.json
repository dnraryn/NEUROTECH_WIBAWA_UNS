{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "Set-Location 'F:\\NEUROTECH'; if (-not $env:GOOGLE_APPLICATION_CREDENTIALS) { Write-Output '[deploy] skipped: GOOGLE_APPLICATION_CREDENTIALS not set'; exit 0 }; if (-not (Get-Command firebase -ErrorAction SilentlyContinue)) { Write-Output '[deploy] skipped: firebase CLI not installed (npm i -g firebase-tools)'; exit 0 }; firebase deploy --only hosting --project neurotech-id --non-interactive",
            "timeout": 180,
            "statusMessage": "Deploying to Firebase Hosting..."
          }
        ]
      }
    ]
  }
}
